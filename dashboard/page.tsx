// CATATAN PENTING:
// Halaman dashboard Next.js (App Router) harus diletakkan di dalam folder `app/`.
// File dashboard yang aktif sekarang berada di:
// -> template/app/dashboard/page.tsx
// 
// File ini (template/dashboard/page.tsx) di luar folder `app` bisa dihapus atau diabaikan.
export default function LegacyDashboardPage() {
  return null;
}
